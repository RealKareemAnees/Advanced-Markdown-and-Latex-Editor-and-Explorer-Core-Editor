import { UpdateHandlerStrategy } from "./UpdateHandler.strategy";

describe("UpdateHandlerStrategy", () => {
    it("should be defined", () => {
        const strategy = new UpdateHandlerStrategy();
        expect(strategy).toBeDefined();
    });
});
